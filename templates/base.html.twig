<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" />

        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    	<div id="sidebar" class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
                <a class="navbar-brand" href="{{ path('app_home')}}" class="nav-link">Gestion des tickets</a>
                {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item active"><a href="{{ path('login') }}" class="nav-link">Se connecter</a></li>
                {% endif %}
                {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item active"><a href="{{ path('app_register') }}" class="nav-link">S'enregistrer</a></li>
                {% endif %}

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item active"><a href="{{ path('ticket.add') }}" class="nav-link">Tickets</a></li>
                {% endif %}

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item active"><a href="{{ path('logout') }}" class="nav-link">Se déconnecter</a></li>
                {% endif %}
                
            </ul>
        </div>
    </nav>
    <div class="container">
        {% block body %}{% endblock %}
        </div>
         <footer>
            <hr/>
            Tickets Application &copy; 2018
        </footer>
        {% block javascripts %}{% endblock %}
</body>
</html>
