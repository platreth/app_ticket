{% extends "base.html.twig" %}
{% block body %}
    <a href="{{ url("ticket.add") }}">Add</a>
    <table class="table">
        <tr>
            <th>Title</th>
            <th>Release On</th>
            <th>Action</th>
        </tr>
        {% for ticket in tickets %}
            <tr>
                <td>{{ ticket.title }}</td>
                <td>{{ ticket.releaseOn|date }}</td>
                <td>
                    <a onclick="return confirm('are you sure?');" href="{{ url("ticket.delete", {"ticket":ticket.id}) }}">Delete</a>
                    <a href="{{ url("ticket.show", {"ticket":ticket.id}) }}">Show</a>
                    <a href="{{ url("ticket.update", {"ticket":ticket.id}) }}">Update</a>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">No content available</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}